{
  "name": "head-tracked-croissant-demo",
  "description": "Head-tracked fixed-object 3D perspective window demo with hand gestures and a spatial box scene.",
  "version": "1.5.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20.19.0"
  },
  "scripts": {
    "start": "node ./scripts/start-demo.mjs",
    "dev": "vite",
    "dev:open": "vite --host 127.0.0.1 --open",
    "build": "vite build",
    "preview": "vite preview",
    "preview:open": "vite preview --host 127.0.0.1 --open",
    "test": "node --test src/gestureMath.test.js src/objectOrientation.test.js",
    "verify": "npm test && npm run build"
  },
  "dependencies": {
    "@mediapipe/tasks-vision": "^0.10.22",
    "three": "^0.183.2"
  },
  "devDependencies": {
    "vite": "^7.1.7"
  }
}
